
# Ignore all .txt and .md files except README.md (case-insensitive)
*.txt
*.TXT
*.md
*.MD
!README.md
!README.MD

The rent and utility costs are usually charged monthly, so adjust the costs to be proportional to real life but for daily (too high right now).
Also improve the UI to have mobile app quality custom graphic buttons, text fonts and color schemes (backgrounds and outlines).
These requests are not complicated, so it shouldn't take too much time.

